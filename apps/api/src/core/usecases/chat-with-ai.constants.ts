/** ハイブリッド検索で取得する上位セクション数（設計書⑥「上位k=8セクション」）。 */
export const RAG_TOP_K = 8;

/** マルチターン文脈としてLLMに渡す直近メッセージ数（コスト・トークン量のバランス）。 */
export const RECENT_MESSAGES_CONTEXT_LIMIT = 10;

/**
 * 免責文言（設計書⑥ガードレール「本回答は参考情報であり規制上の判断はPMDA等へ確認」の定型免責）。
 * LLM出力に依存せずアプリケーション層で確実に付与する。
 */
export const DISCLAIMER_TEXT =
  "本回答は参考情報であり、規制上の判断については各法域の所管当局（日本の場合はPMDA等）へご確認ください。";

/**
 * 検索ヒット0件時の回答拒否テンプレート（設計書⑥「根拠なし回答の禁止、検索ヒット0件時は回答拒否テンプレート」）。
 * この場合はLLMを呼び出さず固定文言のみを返す。
 */
export const NO_HIT_REFUSAL_TEXT =
  "ご質問に関連する法令・通知等の根拠情報が見つからなかったため、回答を控えます。恐れ入りますが質問の表現を変えて再度お試しいただくか、各法域の所管当局（日本の場合はPMDA等）へ直接ご確認ください。";
