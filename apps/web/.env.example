# Next.js (apps/web) 環境変数
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api/v1

# Auth.js (next-auth v5) セッション暗号化用シークレット。
# 生成例: npx auth secret  または  node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=
